import Subtitle from '@/utilities/subtitle/Subtitle';
import React from 'react';
import { ContactSvg } from '@/assets/SvgContainer';

const Contact = () => {
    return (
        <div>
            <Subtitle text="Medios de contacto" Svg={ContactSvg} />
        </div>
    );
};

export default Contact;
